meta {
  name: PATCH /v1/admin/apps/:id
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/v1/admin/apps/{{appId}}
  body: json
  auth: inherit
}

body:json {
  {
    "key_allowlist": ["demo-*", "wdsx-*"]
  }
}
