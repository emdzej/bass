meta {
  name: DELETE /v1/admin/apps/:id/devices/:deviceId
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/v1/admin/apps/{{appId}}/devices/{{deviceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}
