meta {
  name: pairing
  seq: 2
}

auth {
  mode: none
}

docs {
  Public endpoints — they initiate / complete the OIDC code exchange.
  Bass itself drives the OIDC round-trip, so no client-side bearer here.
}
