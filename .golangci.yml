run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused
    - ineffassign
    - gofmt
    - gocritic
    - misspell

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
